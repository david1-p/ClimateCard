# 리팩토링 체크리스트

코드베이스 전체를 검토한 결과입니다.

## ✅ 양호한 부분

### Backend
- ✅ **디버그 코드 없음**: `System.out.println`, `printStackTrace` 없음
- ✅ **TODO/FIXME 없음**: 미완성 코드 없음
- ✅ **적절한 파일 크기**: 가장 큰 파일 197줄 (BusArrivalService.java)
- ✅ **좋은 패키지 구조**: 레이어드 아키텍처 잘 분리됨
- ✅ **보안**: Rate limiting, API key 인증, HSTS 등 완벽 구현

### Frontend
- ✅ **타입 안정성**: TypeScript 잘 활용
- ✅ **컴포넌트 분리**: UI 컴포넌트 잘 분리됨
- ✅ **환경 변수 사용**: 하드코딩 최소화
- ✅ **에러 처리**: Sentry 통합 완료

---

## ⚠️ 개선이 필요한 부분

### 1. 🔴 **CRITICAL: App.tsx 파일 크기 (849줄)**

**문제점:**
- 단일 파일이 너무 큼 (권장: 200-300줄)
- 상태 관리, 비즈니스 로직, UI가 모두 섞여 있음
- 유지보수 어려움, 테스트 어려움

**권장 리팩토링:**
```
frontend/src/
├── hooks/
│   ├── useStations.ts        # 정류소 관련 상태/로직
│   ├── useRoutes.ts           # 노선 관련 상태/로직
│   ├── useArrivals.ts         # 도착정보 관련 상태/로직
│   ├── useSearch.ts           # 검색 관련 상태/로직
│   └── useGeolocation.ts      # 위치 정보 관련
├── components/
│   ├── Map/
│   │   ├── KakaoMap.tsx       # 지도 컴포넌트
│   │   └── StationMarkers.tsx # 마커 컴포넌트
│   ├── Station/
│   │   ├── StationList.tsx    # 정류소 목록
│   │   ├── StationCard.tsx    # 정류소 카드
│   │   └── RouteList.tsx      # 노선 목록
│   └── Route/
│       ├── RouteDetails.tsx   # 노선 상세
│       └── StationTimeline.tsx # 정류소 타임라인
└── App.tsx                    # 레이아웃만 담당 (~100줄)
```

**예상 효과:**
- 가독성 향상
- 테스트 용이성
- 재사용성 증가
- 팀 협업 개선

---

### 2. 🟡 **MEDIUM: scripts 디렉토리 정리 필요**

**현황:**
- Python 스크립트: 26개
- 많은 스크립트가 유사한 기능 수행

**중복 가능성 있는 스크립트:**
```
- crawl_bus_stations.py
- crawl_bus_route_stations.py
- crawl_busgo_route_stations.py
  → 크롤링 스크립트 통합 가능

- sync_missing_route_stations.py
- sync_route_stations.py
- sync_via_station_api.py
  → 동기화 스크립트 통합 가능

- load_climate_routes.py
- update_climate_routes.py
- add_all_seoul_routes.py
  → 노선 로드 스크립트 통합 가능
```

**권장 구조:**
```
scripts/
├── data_import/
│   ├── import_routes.py       # 노선 가져오기 (통합)
│   ├── import_stations.py     # 정류소 가져오기 (통합)
│   └── import_excel.py        # 엑셀 데이터 가져오기
├── sync/
│   ├── sync_routes.py         # 노선 동기화 (통합)
│   └── sync_stations.py       # 정류소 동기화 (통합)
├── maintenance/
│   ├── add_express_buses.py   # 광역버스 추가 (보관)
│   └── add_gyeonggi_incheon_buses.py # 경기/인천 버스 추가 (보관)
└── archive/                   # 사용 안 하는 스크립트
```

---

### 3. 🟢 **LOW: 사소한 개선사항**

#### a) Sentry console.log 제거 가능
**파일:** `frontend/src/sentry.ts:14`
```typescript
// 현재
console.log('Sentry DSN이 설정되지 않았습니다...');

// 개선 (production에서만 제거)
if (import.meta.env.DEV) {
  console.log('Sentry DSN이 설정되지 않았습니다...');
}
```

#### b) logger.ts의 any 타입
**파일:** `frontend/src/utils/logger.ts`
```typescript
// 현재
log: (...args: any[]) => {...}

// 개선
log: (...args: unknown[]) => {...}
```

#### c) BottomSheet의 any 타입
**파일:** `frontend/src/components/Layout/BottomSheet.tsx:45`
```typescript
// 현재
const onDragEnd = useCallback((_: any, info: PanInfo) => {...}

// 개선
const onDragEnd = useCallback((_event: MouseEvent | TouchEvent | PointerEvent, info: PanInfo) => {...}
```

---

## 📊 우선순위

1. **🔴 HIGH (즉시)**: App.tsx 리팩토링
   - 예상 작업 시간: 2-3시간
   - 효과: 매우 높음
   - 위험도: 중간 (충분한 테스트 필요)

2. **🟡 MEDIUM (1주일 내)**: scripts 정리
   - 예상 작업 시간: 1-2시간
   - 효과: 중간
   - 위험도: 낮음 (스크립트만 영향)

3. **🟢 LOW (여유 있을 때)**: 사소한 개선
   - 예상 작업 시간: 30분
   - 효과: 낮음
   - 위험도: 매우 낮음

---

## 🎯 권장 작업 순서

### Phase 1: App.tsx 리팩토링
1. Custom Hook 분리
2. 컴포넌트 분리
3. 기능 테스트

### Phase 2: Scripts 정리
1. 사용 중인 스크립트 파악
2. 중복 스크립트 통합
3. archive 디렉토리로 이동

### Phase 3: 사소한 개선
1. any 타입 개선
2. console.log 정리

---

## 💡 추가 제안

### 성능 최적화
- ✅ 이미 완료: Nearby API LIMIT 50
- ✅ 이미 완료: 불필요한 필드 제거
- 🔄 고려 사항: React.memo(), useMemo() 활용

### 접근성 (a11y)
- 키보드 네비게이션 지원
- ARIA 레이블 추가
- 색상 대비 개선

### 테스트
- 백엔드 테스트 재작성
- Frontend E2E 테스트 추가 (Playwright, Cypress)

---

**마지막 업데이트**: 2025-12-30
**작성자**: Claude Code
